tags:
  - two_duts

multi_device:
  device0: master
  device1: slave

platforms:
  # Wokwi and QEMU do not support multi-device tests
  wokwi: false
  qemu: false

requires:
  - CONFIG_SOC_WIFI_SUPPORTED=y
