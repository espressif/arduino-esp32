tags:
  - generic_multi_device

multi_device:
  device0: server
  device1: client

platforms:
  # Wokwi and QEMU do not support multi-device tests
  wokwi: false
  qemu: false
  # Hardware runners not yet assigned
  hardware: false

requires:
  - CONFIG_SOC_BLE_SUPPORTED=y
